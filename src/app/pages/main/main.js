import {
  header,
  about,
  portfolios,
  skills,
  experience,
  education,
  contact,
} from "../../layouts";

let mainHTML =
  header + about + portfolios + skills + experience + education + contact;

export { mainHTML };
