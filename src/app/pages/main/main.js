import {
  header,
  about,
  portfolios,
  skills,
  experiance,
  education,
  contact,
} from '../../layouts';

let mainHTML =
  header + about + portfolios + skills + experiance + education + contact;

export { mainHTML };
