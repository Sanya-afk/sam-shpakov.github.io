import {
  header,
  about,
  portfolio,
  skills,
  experience,
  education,
  contact,
} from '../../layouts';

let mainHTML =
  header + about + portfolio + skills + experience + education + contact;

export { mainHTML };
